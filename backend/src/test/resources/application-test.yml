spring:
  kafka:
    bootstrap-servers: ${spring.embedded.kafka.brokers:localhost:9092}

vectramoment:
  storage:
    local-dir: ${java.io.tmpdir}/vectramoment-test
  opensearch:
    endpoint: http://localhost:9200
